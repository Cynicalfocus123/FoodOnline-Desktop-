import { useEffect, useState, type ReactNode } from "react";
import { ApiError } from "../../lib/apiClient";
import { operationsApi } from "../../services/admin/operationsApi";

const errorOf = (error: unknown) => error instanceof ApiError ? error.message : "Request failed.";
function Shell({ title, children }: { title: string; children: ReactNode }) { return <section className="rounded-[32px] border border-neutral-200 bg-white p-6 shadow-soft sm:p-8"><p className="text-sm font-black uppercase tracking-[0.2em] text-citrus-500">Operations</p><h2 className="mt-3 text-3xl font-black">{title}</h2>{children}</section>; }

export function ReturnsAdminPanel({ token }: { token: string }) { const [rows, setRows] = useState<Array<Record<string, unknown>>>([]); const [message, setMessage] = useState("Loading returns..."); const load = () => operationsApi.returns(token).then((r) => { setRows(r.data); setMessage(r.data.length ? "" : "No return requests."); }).catch((e) => setMessage(errorOf(e))); useEffect(() => { void load(); }, [token]); return <Shell title="Returns management"><p className="mt-3 text-sm text-neutral-600">Inspect returned grocery items and choose restock quantities explicitly. COD refunds are manual records.</p>{message && <p className="mt-4 rounded-2xl bg-neutral-50 p-3 text-sm">{message}</p>}<div className="mt-6 grid gap-3">{rows.map((row) => <div className="rounded-2xl border p-4" key={String(row.uuid)}><div className="flex flex-wrap justify-between gap-3"><p className="font-black">{String(row.return_number)} · {String(row.status)}</p><div className="flex gap-2">{["approve", "received", "refund", "close"].map((action) => <button className="rounded-full border px-3 py-1 text-xs font-black" key={action} onClick={() => void operationsApi.returnAction(token, String(row.uuid), { action, amount_minor: action === "refund" ? Number(window.prompt("Manual refund minor units") ?? 0) : undefined }).then(load).catch((e) => setMessage(errorOf(e)))} type="button">{action}</button>)}</div></div><p className="mt-2 text-sm text-neutral-600">Order: {String((row.order as Record<string, unknown> | undefined)?.order_number ?? "—")}</p></div>)}</div></Shell>; }
export function ReviewsAdminPanel({ token }: { token: string }) { const [rows, setRows] = useState<Array<Record<string, unknown>>>([]); const [message, setMessage] = useState("Loading reviews..."); const load = () => operationsApi.reviews(token).then((r) => { setRows(r.data); setMessage(r.data.length ? "" : "No reviews awaiting moderation."); }).catch((e) => setMessage(errorOf(e))); useEffect(() => { void load(); }, [token]); return <Shell title="Review moderation"><p className="mt-3 text-sm text-neutral-600">Published aggregates use only approved Laravel reviews. Reported content remains visible until moderated.</p>{message && <p className="mt-4 rounded-2xl bg-neutral-50 p-3 text-sm">{message}</p>}<div className="mt-6 grid gap-3">{rows.map((row) => <article className="rounded-2xl border p-4" key={String(row.uuid)}><div className="flex justify-between gap-3"><p className="font-black">{"★".repeat(Number(row.rating ?? 0))} · {String(row.status)}</p><div className="flex gap-2">{["approve", "reject", "hide", "restore"].map((action) => <button className="rounded-full border px-3 py-1 text-xs font-black" key={action} onClick={() => void operationsApi.reviewAction(token, String(row.uuid), { action }).then(load).catch((e) => setMessage(errorOf(e)))} type="button">{action}</button>)}</div></div><p className="mt-2 text-sm">{String(row.body ?? "")}</p><p className="mt-2 text-xs text-neutral-500">{String((row.user as Record<string, unknown> | undefined)?.email ?? "Customer")} · Verified: {row.verified_purchase ? "Yes" : "No"}</p></article>)}</div></Shell>; }
export function SupportAdminPanel({ token }: { token: string }) { const [rows, setRows] = useState<Array<Record<string, unknown>>>([]); const [message, setMessage] = useState("Loading support tickets..."); useEffect(() => { void operationsApi.support(token).then((r) => { setRows(r.data); setMessage(r.data.length ? "" : "No support tickets."); }).catch((e) => setMessage(errorOf(e))); }, [token]); return <Shell title="Customer support"><p className="mt-3 text-sm text-neutral-600">Internal messages stay server-side; replies marked customer-visible are shown to the customer.</p>{message && <p className="mt-4 rounded-2xl bg-neutral-50 p-3 text-sm">{message}</p>}<div className="mt-6 grid gap-3">{rows.map((row) => <div className="rounded-2xl border p-4" key={String(row.uuid)}><p className="font-black">{String(row.ticket_number)} · {String(row.status)}</p><p className="mt-2 text-sm">{String(row.subject)}</p><button className="mt-3 rounded-full bg-leaf-600 px-4 py-2 text-xs font-black text-white" onClick={() => { const body = window.prompt("Reply"); if (body) void operationsApi.supportMessage(token, String(row.uuid), { body, customer_visible: true }); }} type="button">Reply</button></div>)}</div></Shell>; }
export function ReportsAdminPanel({ token }: { token: string }) { const [report, setReport] = useState<Record<string, unknown> | null>(null); const [message, setMessage] = useState("Loading reports..."); useEffect(() => { void operationsApi.reports(token).then(setReport).catch((e) => setMessage(errorOf(e))); }, [token]); return <Shell title="Reports and exports"><p className="mt-3 text-sm text-neutral-600">Paid COD and unpaid COD are separated. CSV exports are permission protected.</p>{message && !report && <p className="mt-4 rounded-2xl bg-neutral-50 p-3 text-sm">{message}</p>}{report ? <pre className="mt-6 max-h-[28rem] overflow-auto rounded-2xl bg-neutral-950 p-4 text-xs text-emerald-100">{JSON.stringify(report, null, 2)}</pre> : null}<button className="mt-5 inline-flex rounded-full bg-neutral-900 px-5 py-3 text-sm font-black text-white" onClick={() => void operationsApi.downloadOrdersCsv(token).catch((e) => setMessage(errorOf(e)))} type="button">Export order CSV</button></Shell>; }
export function StaffAdminPanel({ token }: { token: string }) { const [rows, setRows] = useState<Array<Record<string, unknown>>>([]); useEffect(() => { void operationsApi.staff(token).then((r) => setRows(r.data)).catch(() => setRows([])); }, [token]); return <Shell title="Staff permissions and MFA"><p className="mt-3 text-sm text-neutral-600">Authorization is enforced by backend permission middleware. Existing administrators remain super administrators until explicitly assigned a narrower role.</p><div className="mt-6 grid gap-3">{rows.map((row) => <div className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border p-4" key={String(row.id)}><span><strong>{String(row.name)}</strong> · {String(row.email)}<small className="ml-2 text-neutral-500">{String(row.role)} · MFA {row.mfa_enabled ? "on" : "off"}</small></span><select className="rounded-full border px-3 py-2 text-xs font-black" defaultValue={String(row.role)} onChange={(e) => void operationsApi.updateStaff(token, Number(row.id), { staff_role: e.target.value }).catch(() => undefined)}><option>super_admin</option><option>order_manager</option><option>inventory_manager</option><option>catalog_manager</option><option>customer_support</option><option>marketing_manager</option><option>read_only</option></select></div>)}</div></Shell>; }
export function OperationsAdminPanel({ token }: { token: string }) { const [status, setStatus] = useState<Record<string, unknown> | null>(null); const [jobs, setJobs] = useState<Array<{ uuid: string; queue: string; failed_at: string; exception: string }>>([]); const [message, setMessage] = useState(""); const loadJobs = () => operationsApi.failedJobs(token).then((result) => setJobs(result.data)).catch((e) => setMessage(errorOf(e))); useEffect(() => { void operationsApi.operations(token).then(setStatus).catch(() => setStatus(null)); void loadJobs(); }, [token]); return <Shell title="System health"><p className="mt-3 text-sm text-neutral-600">Safe diagnostics expose statuses and timestamps only; credentials, DSNs, and raw exception traces are never returned.</p><pre className="mt-6 overflow-auto rounded-2xl bg-neutral-950 p-4 text-xs text-emerald-100">{status ? JSON.stringify(status, null, 2) : "Loading..."}</pre><div className="mt-6 rounded-2xl border border-neutral-200 p-4"><p className="font-black">Failed jobs ({jobs.length})</p>{message ? <p className="mt-2 text-sm text-red-700">{message}</p> : null}{jobs.length ? jobs.map((job) => <div className="mt-3 flex flex-wrap items-center justify-between gap-3 border-t border-neutral-100 pt-3" key={job.uuid}><span className="min-w-0 text-xs"><strong>{job.queue}</strong> · {new Date(job.failed_at).toLocaleString()}<br /><span className="text-neutral-500">{job.exception}</span></span><button className="rounded-full border border-leaf-500 px-3 py-2 text-xs font-black text-leaf-700" onClick={() => void operationsApi.retryFailedJob(token, job.uuid).then(loadJobs).catch((e) => setMessage(errorOf(e)))} type="button">Retry</button></div>) : <p className="mt-2 text-sm text-neutral-500">No failed jobs retained.</p>}</div></Shell>; }
